class DurationConstants {
  DurationConstants._();

  static const defaultSnackBarDuration = Duration(seconds: 3);
  static const maxTimeLiveOTP = Duration(minutes: 3);

  static const defaultIndicatorRefreshDuration = Duration(milliseconds: 500);
}
